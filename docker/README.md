# Docker

Local development services live in the root `docker-compose.yml`.

Current services:

- Postgres on host port `54329`
- Redis on host port `6380`

These services support the Laravel scaffold in `apps/server`. Production self-hosting templates will be added after the first working Community Edition build exists.

License: MIT. See [LICENSE](LICENSE).
